interface:
  display_name: "Reactive Resume"
  short_description: "Repo-specific patterns and workflows for reactive-resume"
  default_prompt: "Use the reactive-resume repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true